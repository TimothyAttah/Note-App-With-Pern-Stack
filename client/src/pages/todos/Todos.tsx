import React, { FC } from 'react';
import ListsTodos from './ListsTodos';

const Todos: FC = () => {
	return (
		<div>
      <h1>Todos Page</h1>
      <ListsTodos />
		</div>
	);
};

export default Todos;
